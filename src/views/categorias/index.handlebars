<h1>Categorias</h1>

{{#each categorias}}

<div class="card bg-light shadow pt-1 bg-body rounded bg-gradient mt-4">
    <div class="card-body">
        <a href="/categorias/{{slug}}" style="text-decoration: none">
            <h4>{{nome}}</h4>
        </a>
    </div>
</div>
{{else}}
<h4>Nenhuma categoria registrada.</h4>
{{/each}}